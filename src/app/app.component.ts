import { Component } from '@angular/core';
import { Compression } from './common/util/compression';

@Component({
  selector: 'app-root',
  templateUrl: './app.template.html',
  styleUrls: ['./app.scss'],
})
export class AppComponent {
  title = 'looping-ages';
}
